# Whisper_live_transcription_streamlit
Install the requirements file and proceed with the streamlit initialize command in terminal


# installation command
pip3 install -r requirements.txt

after installation
streamlit run main.py
